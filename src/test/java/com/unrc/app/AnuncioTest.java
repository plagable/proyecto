package com.unrc.app;
    
import org.junit.Test;
  import static org.junit.Assert.*;
  import com.unrc.app.*;
  import org.javalite.activejdbc.Model;

public class AnuncioTest  {
    
    // valores de prueba
    private String contenido = "casa 2 hab";
   
  //   Anuncio expected =  Anuncio.getId(); // el get dle model 
     Anuncio obteneid = new Anuncio(); 
     
     //public void insertar(String descriptive_text,int id_buildings, int id_ownersBuildings){
     @Test  
    public void testInsertar () {                  
     // Building inm1 = Building.getId();
      
      //if (existeInmueble && existe dueño) then
              
    // assertEquals(expected,obteneid.insertar(contenido,2,1));
     //assertEquals(expected,obteneid.insertar(contenido,2,1));
     //assertEquals(expected,obteneid.insertar(contenido,2,1));
     
     } 
        

 }


